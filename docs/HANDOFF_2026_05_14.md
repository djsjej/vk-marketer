# HANDOFF — vk-marketer, 14 мая 2026, конец сессии

**Цель документа:** дать новому чату Claude полный контекст без потери. Эта
итерация заменяет `HANDOFF_2026_05_13.md`.

-----

## ⚠️ Перед началом работы — проверь PAT

PAT от GitHub истекает 15 мая 2026. Если истёк — Vizit выпускает новый
по инструкции из старого HANDOFF, **обязательно** с правами
**Repository → Contents: Read AND Write** (в прошлый раз эту галочку
пропустили, push не работал).

-----

## ✅ Что сделано в сессии 14 мая (4 коммита)

| Хэш       | Что                                                                      |
|-----------|--------------------------------------------------------------------------|
| `8c14455` | fix(bot): восстановил `help_command` — worker крашился `ImportError`     |
| `0eaef30` | feat(biba): `/biba` шлёт ZIP с raw JSON всех 36 endpoints                |
| `6096553` | feat(phase4): package 3127 + новый таргетинг + env `VK_AUDIENCE_SEGMENT_IDS` |
| `58b0358` | feat(phase4): `interests_soc_dem` расширен до 3 ID (OR-логика)           |

Финальный коммит: **`58b0358`**.

### Что бот сейчас передаёт в каждую группу (через API)

```python
targetings = {
    "geo": {"regions": [188]},        # Россия
    "sex": ["female"],                # Женщины
    "age": {"age_list": [0, 41..58]}, # 6 возрастных окон, по 3 года
    "interests_soc_dem": [
        27137,  # Духовный рост и самовыражение
        10186,  # Женаты, замужем
        12920,  # Есть дети в семье
    ],
    "segments": [...]  # из env VK_AUDIENCE_SEGMENT_IDS, опционально
}
# package_id = 3127 (Написать в сообщество)
# cta_community_vk = "write"
```

**Логика выбора:** OR внутри `interests_soc_dem` (расширение для oCPM-обучения),
AND между полями (`sex × age × geo` сужает). Источник истины полей —
`docs/VK_API_Targetings.md` (присланная Vizit'ом 14.05 страница доки).

-----

## 🚨 Что осталось от Vizit'а (open action items)

Ничего из этого я (Claude) сам сделать не могу — нужны действия Vizit'а
в UI кабинета VK Ads и в Railway.

1. **Создать новую template-кампанию в кабинете VK Ads** под package 3127
   «Написать в сообщество». Минимум: одна группа с одним тестовым
   объявлением (это нужно VK чтобы инициализировать `patterns` в
   settings package — без них API создавать группы не даёт). Интересы
   в template можно не настраивать — они не наследуются нашими
   API-группами.
2. **Обновить в Railway** `VK_TEMPLATE_AD_PLAN_ID` на ID новой кампании
   (заменить старый `20865519`, который был под package 3122).
3. **(Опционально) создать сегмент** в разделе «Аудитории» с подписчиками
   правосл. сообществ → ID в Railway `VK_AUDIENCE_SEGMENT_IDS`
   (CSV формата `12345,67890`).
4. **Тестовый прогон**: отправить боту фото с темой про молитвы за
   близких, проверить что кампания создалась с package 3127, CTA
   «Написать», 6 групп по возрастам с новыми targetings.

**Пока шаги 1-2 не сделаны** — бот по-прежнему добавляет группы в старую
template-кампанию `20865519` (package 3122 «Вступить»), потому что
package диктуется ad_plan'ом, а не targetings новой группы.

### Список православных VK сообществ для стартового сегмента (шаг 3)

Из ТОП-10 на pravoslavie.fm (статья 2016, базовые сообщества скорее всего
живы). Vizit при создании сегмента в UI проверяет существование URL.

| URL                                | Название                | Размер на 2016 |
|------------------------------------|-------------------------|----------------|
| `vk.com/pravoslavnie_hristiane`    | Верую † Православие     | 700k+          |
| `vk.com/orthodox_woman`            | Православная женщина    | 80k+           |
| `vk.com/ortodoxia`                 | Православие † Ορθόδοξη  | 85k+           |
| `vk.com/pravoclavie`               | Православие             | 80k+           |
| `vk.com/p_trapeza`                 | Постная трапеза         | 90k+           |
| `vk.com/orthodoxpsiholog`          | Православная психология | 55k+           |

Активность подписчиков (комментирование) — отдельная задача под TargetHunter,
см. Phase 5 ниже.

-----

## 🎯 Phase 5 — Спецификация Таргетолога-агента (от Vizit'а 14.05)

Vizit предложил отдельного суб-агента под подключение к **TargetHunter**.
Это внешний сервис который парсит подписчиков VK сообществ и собирает
сегменты по поведению (активные, комментирующие, лайкающие). TargetHunter
выгружает CSV → Vizit грузит в VK Ads «Аудитории» → получается `segments` ID.

### Что должен делать Таргетолог

1. **Принимает от Vizit'а** список целевых VK-сообществ (через Telegram
   команду или env var).
2. **Через TargetHunter API** (нужен API-ключ от Vizit'а, новая env var):
   - Парсит подписчиков всех групп
   - Ищет **пересечения** — кто подписан на 2+ нишевых сообщества
     (самая горячая аудитория для нашей ниши)
   - Фильтрует на **активность** — кто реально комментирует/лайкает
     в выбранный период
3. **Креативит**: предлагает Vizit'у через Claude N разных «связок»
   (группа+интерес) для A/B-тестов. Например:
   - «Подписчики Дивеево + Богородичные иконы»
   - «Активные комментаторы Веруй+Православие + молитвы за здравие»
4. **Выгружает результат** как CSV (готовый для загрузки в VK Ads
   «Аудитории» → сегмент).
5. **Подсказывает Vizit'у в Telegram**: «загрузи этот CSV в Аудитории,
   потом скинь мне ID сегмента, я положу его в `VK_AUDIENCE_SEGMENT_IDS`».

### Где брать данные про TargetHunter API

Vizit пришлёт в новом чате:
- Документация TargetHunter API
- API-ключ (через PAT-style выдачу, не в репо)

### Архитектура (черновик)

```
src/targetolog/
├── __init__.py
├── persona.py          # личность агента (как у Бибы)
├── client.py           # HTTP-клиент TargetHunter API
├── intersections.py    # поиск пересечений подписчиков групп
├── activity_filter.py  # фильтр на комментаторов/лайкеров
├── creative_brain.py   # Claude генерит связки группа+интерес
└── csv_export.py       # выгрузка готового CSV под VK Ads формат

src/telegram_bot/handlers.py:
    добавить /targetolog команду

config.py:
    добавить TARGETHUNTER_API_KEY
```

### Принципы (по INSTRUCTIONS v1.2)

- Один агент = одна задача (Таргетолог НЕ лезет в API VK Ads — это
  моё дело и Бибы).
- Read-only для VK; write-only для своих CSV-файлов.
- Не инициирует POST/PUT в VK без подтверждения Claude.
- Покрыт тестами через respx-моки (как у Бибы).

-----

## 📚 Документация в репо (читать первым в новом чате)

| Файл                              | Что внутри                                                |
|-----------------------------------|-----------------------------------------------------------|
| `docs/INSTRUCTIONS.md` (v1.2)     | **Обязательно** — workflow, роли, принцип суб-агентов     |
| `docs/HANDOFF_2026_05_14.md`      | Этот файл                                                 |
| `docs/VK_API_Targetings.md`       | Все поля targetings (от Vizit'а 14.05)                    |
| `docs/VK_API_AdPlans.md`          | Создание кампании                                         |
| `docs/VK_API_AdGroups.md`         | Создание группы                                           |
| `docs/VK_API_Package.md`          | Объект пакета                                             |
| `docs/KNOWN_GOOD_PAYLOAD.md`      | Рабочий payload-эталон                                    |

-----

## 🧠 Урок предыдущей сессии (важно для нового чата)

**`HANDOFF_2026_05_13.md` был не точен.** Там было написано «Биба упал
в обморок» — это диагноз прошлой сессии. На самом деле выяснилось что
worker вообще не стартовал (`ImportError: cannot import name 'help_command'`),
и сообщение `Биба упал в обморок` Vizit видел от старого деплоя ещё
**до** того как был сломан импорт.

**Вывод для нового Claude:** диагнозы предыдущих сессий — это гипотезы,
не факты. Перед записью в документацию **подтверждать** тестом или
логом. Соответствующее правило добавлено в `docs/INSTRUCTIONS.md`.

-----

## 📞 Контакт с Vizit'ом

- Telegram, iPhone, мобильно
- Скрины и копипасту присылает в чат
- Кампании создаёт через Telegram-бот (`@Zkakcjwkkxnrbot`)
- Управляет env vars в Railway UI
- API-ключи (PAT, TargetHunter) присылает в чат лично, не в репо
